# civic-voice-app — optional local overrides (copy to `.env`; never commit `.env`).
#
# Firebase Admin (optional for engine scripts that read/write Firestore):
# Do NOT use tutorial placeholders such as PASTE_THE_FULL_PATH_HERE — remove the line or set a real path.
# Prefer loading scheduler credentials from `../civic-voice-engine/.env` (FIREBASE_PROJECT_ID + …).
#
# GOOGLE_APPLICATION_CREDENTIALS=C:\absolute\path\to\your-service-account.json
